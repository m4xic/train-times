/**
 * Preset quick routes shown on the landing page.
 * Add/edit entries here to customise the home screen shortcuts.
 *
 * Each entry:
 *   label   – short display label (e.g. "Home", "Work", "Commute")
 *   from    – { name, crs }  departure station
 *   to      – { name, crs }  (optional) pre-applied destination filter
 */
export const presets = []
